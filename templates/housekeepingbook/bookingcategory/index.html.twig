{% extends 'theme/base-datatables.html.twig' %}

{% block page_header_buttons %}
    {% if is_granted('createBookingCategory', household) %}
        <a href="{{ path('housekeepingbook_bookingcategory_new') }}" class="btn btn-outline-primary btn-sm btn-icon-split ml-3">
            <span class="icon text-primary mr-1">
              <i class="fas fa-plus-circle"></i>
            </span>
            <span class="text">{{ 'Add booking category...'|trans }}</span>
        </a>
    {% endif %}
{% endblock %}

{% block page_content %}
    <div class="row">
        <div class="col-lg-12">
            <div class="card card-primary">
                <div class="card-header">
                    <h3 class="card-title">{{ 'Booking categories'|trans }}</h3>
                </div>
                <div class="card-body">
                    <table id="bookingCategories" class="table table-hover table-striped dataTable" role="grid">
                        <thead>
                        <tr>
                            <th>{{ 'Name'|trans }}</th>
                            <th>{{ 'CreatedAt'|trans }}</th>
                            <th></th>
                        </tr>
                        </thead>
                        {% if bookingCategories %}
                            <tbody>
                            {% for bookingCategory in bookingCategories %}
                                <tr>
                                    <td>{{ bookingCategory.name }}</td>
                                    <td class="min">{{ bookingCategory.createdAt }}</td>
                                    <td class="text-right min">
                                        <div class="btn-group">
                                            {% if is_granted('edit', bookingCategory) %}
                                                <a href="{{ path('housekeepingbook_bookingcategory_edit', {'id': bookingCategory.id}) }}" class="btn btn-xs btn-outline-primary">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                            {% endif %}
                                        </div>
                                        <div class="btn-group">
                                            {% if is_granted('delete', bookingCategory) %}
                                                <form method="post" action="{{ path('housekeepingbook_bookingcategory_delete', {'id': bookingCategory.id}) }}" onsubmit="return confirm('{{ 'Are you sure you want to delete this item?'|trans }}');">
                                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bookingCategory.id) }}">
                                                    <button class="btn btn-xs btn-outline-danger btn-delete"><i class="fas fa-trash-alt"></i></button>
                                                </form>
                                            {% endif %}
                                        </div>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="3">{{ 'no records found'|trans }}</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        {% endif %}
                    </table>
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div>
    </div>
    <!-- /.row -->
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    {{ encore_entry_script_tags('housekeepingbook_bookingcategory_index') }}
{% endblock javascripts %}