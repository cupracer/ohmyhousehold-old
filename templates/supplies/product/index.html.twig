{% extends 'theme/base-datatables.html.twig' %}

{% block page_header_buttons %}
    {% if is_granted('createSuppliesProduct', household) %}
        <a href="{{ path('supplies_product_new') }}" class="btn btn-outline-primary btn-sm btn-icon-split ml-3">
            <span class="icon text-primary mr-1">
              <i class="fas fa-plus-circle"></i>
            </span>
            <span class="text">{{ 'Add product...'|trans }}</span>
        </a>
    {% endif %}
{% endblock %}

{% block page_content %}
    <div class="row">
        <div class="col-lg-12">
            <div class="card card-primary">
                <div class="card-header">
                    <h3 class="card-title">{{ 'Products'|trans }}</h3>
                </div>
                <div class="card-body">
                    <table id="products" class="table table-hover table-striped dataTable" role="grid"
                        data-json-url="{{ path('supplies_product_datatables') }}"
                           data-i18n-url="{{ path('app_datatables_locale') }}">
                        <thead>
                        <tr>
                            <th>{{ 'Name'|trans }}</th>
                            <th>{{ 'Brand'|trans }}</th>
                            <th>{{ 'EAN'|trans }}</th>
                            <th>{{ 'Category'|trans }}</th>
                            <th>{{ 'Packaging'|trans }}</th>
                            <th>{{ 'Amount'|trans }}</th>
                            <th>{{ '#stock'|trans }}</th>
                            <th></th>
                            <th></th>
                        </tr>
                        </thead>
                    </table>
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div>
    </div>
    <!-- /.row -->
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    {{ encore_entry_script_tags('supplies_product_index') }}
{% endblock javascripts %}