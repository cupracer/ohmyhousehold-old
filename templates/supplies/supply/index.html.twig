{% extends 'theme/base-datatables.html.twig' %}

{% block page_header_buttons %}
    {% if is_granted('createSuppliesSupply', household) %}
        <a href="{{ path('supplies_supply_new') }}" class="btn btn-outline-primary btn-sm btn-icon-split ml-3">
            <span class="icon text-primary mr-1">
              <i class="fas fa-plus-circle"></i>
            </span>
            <span class="text">{{ 'Add supply...'|trans }}</span>
        </a>
    {% endif %}
{% endblock %}

{% block page_content %}
    <div class="row">
        <div class="col-lg-12">
            <div class="card card-primary">
                <div class="card-header">
                    <h3 class="card-title">{{ 'Supplies'|trans }}</h3>
                </div>
                <div class="card-body">
                    <table id="supplies" class="table table-hover table-striped dataTable" role="grid"
                        data-json-url="{{ path('supplies_supply_datatables') }}">
                        <thead>
                        <tr>
                            <th>{{ 'Name'|trans }}</th>
                            <th>{{ 'Category'|trans }}</th>
                            <th>{{ 'Minimum #'|trans }}</th>
                            <th>{{ '#stock'|trans }}</th>
                            <th></th>
                        </tr>
                        </thead>
                    </table>
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div>
    </div>
    <!-- /.row -->
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    {{ encore_entry_script_tags('supplies_supply_index') }}
{% endblock javascripts %}